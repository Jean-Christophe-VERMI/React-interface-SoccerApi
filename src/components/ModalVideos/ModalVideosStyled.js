import styled from 'styled-components';

const ModalVideosStyled = styled.div`

  position: absolute;
  width: 400;
  background: ${({ theme }) => theme.background};
  top: 50%;
  Left: 50%;

  
`;

export default ModalVideosStyled;